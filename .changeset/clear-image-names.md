---
"@opencode-ai/browser-control": patch
---

Include visible image alt text in compact snapshot labels and clarify that explicit execute target selection is per-call. Keep Effect and its shared Node adapter aligned in standalone runtime installations so clean npm installs can start successfully.
