---
"@opencode-ai/browser-control": minor
---

Add a package-managed OpenCode plugin entrypoint that registers Browser Control's
bundled MCP server and exposes its packaged workflow through the existing
`@browser` skill mention.
